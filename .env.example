# Local dev: copy to .env and paste your Mapbox public token (pk.…).
# Vercel: set MAPBOX_TOKEN in Project → Settings → Environment Variables instead.
# Also copy assets/config.example.js → assets/config.js for local preview.
MAPBOX_TOKEN=
